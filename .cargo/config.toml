# Configuration for WASM target
[target.wasm32-unknown-unknown]
# For getrandom 0.3+, enable wasm_js backend
rustflags = ["--cfg", "getrandom_backend=\"wasm_js\""]

[build]
# Ensure we use the correct target for wasm-pack
target = "wasm32-unknown-unknown"

